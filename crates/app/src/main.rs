fn main() {
    // Tauri 2 app entry point
    // Will be replaced when we run `cargo tauri init` to scaffold
    // the full Tauri project with web frontend
    println!("Steadfirm app — run `cargo tauri init` to scaffold");
}
